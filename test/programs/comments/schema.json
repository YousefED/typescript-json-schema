{
    "description": "Description of MyObject, a top level object,\n\nwhich also has a comment that spans\n\nmultiple lines",
    "additionalProperties": false,
    "type": "object",
    "properties": {
        "opacity": {
            "description": "Description of opacity, a field with min/max values",
            "minimum": 0,
            "maximum": 100,
            "type": "number"
        },
        "position": {
            "$ref": "#/definitions/Vector3D",
            "description": "Description of field position, of aliased type Vector3D, which should inherit its annotations"
        },
        "rotation": {
            "description": "Description of rotation, a field with an anonymous type",
            "type": "object",
            "properties": {
                "yaw": {
                    "description": "Description of the value yaw inside an anonymous type, with min/max annotations",
                    "minimum": -90,
                    "maximum": 90,
                    "type": "number"
                }
            },
            "required": [
                "yaw"
            ]
        }
    },
    "required": [
        "opacity",
        "position",
        "rotation"
    ],
    "definitions": {
        "Vector3D": {
            "description": "Description of Vector3D, a type alias to a array of integers with length 3\n\nIf run without useTypeAliasRef, this comment should be ignored but\n\nthe other annotations should be inherited",
            "minItems": 3,
            "maxItems": 3,
            "type": "array",
            "items": {
                "$ref": "#/definitions/Vector3D"
            }
        }
    },
    "$schema": "http://json-schema.org/draft-04/schema#"
}
