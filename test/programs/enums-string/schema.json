{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "definitions": {
        "Enum": {
            "enum": [
                "x",
                "y"
            ],
            "type": "string"
        }
    },
    "properties": {
        "foo": {
            "$ref": "#/definitions/Enum"
        }
    },
    "required": [
        "foo"
    ],
    "type": "object"
}

